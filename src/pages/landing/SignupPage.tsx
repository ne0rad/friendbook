import { FormBox, MainBox, SignupForm } from "./components";

export default function LoginPage(): JSX.Element {
  return (
    <MainBox>
      <FormBox>
        <SignupForm />
      </FormBox>
    </MainBox>
  );
}
